[server]
listen_addr = "0.0.0.0"
listen_port = 8080
max_connections = 100

[mumble]
host = "localhost"
port = 64738
accept_invalid_certs = true

[webrtc]
stun_servers = ["stun:stun.l.google.com:19302"]
